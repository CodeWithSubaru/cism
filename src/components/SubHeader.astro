---
interface Props {
  className?: string;
  style?: string;
}

const { className, style } = Astro.props;
---

<h2
  class=`md:text-4xl text-3xl leading-tight md:mb-4 mb-3 text-white ${className}`
  style={style}
>
  <slot />
</h2>
